<% alert_keys = { alert: 'danger', notice: 'primary', success: 'success'} %>

<% flash.each do |key, value| %>
  <%= content_tag :div, value.html_safe, class: "flash alert alert-#{alert_keys[key.to_sym]}" %>
<% end %>
