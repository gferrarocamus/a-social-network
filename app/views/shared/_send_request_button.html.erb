<%= form_tag("/requests", method: "post", remote: true) do %>
  <%= hidden_field_tag :receiver_id, user.id %>
  <%= submit_tag("Send friend request", class: 'btn btn-sm button blue-button') %>
<% end %>